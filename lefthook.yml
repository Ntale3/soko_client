pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: pnpm lint:fix {staged_files}
    format:
      glob: "*.{ts,tsx,js,jsx,json,css,md}"
      run: pnpm prettier --write {staged_files}